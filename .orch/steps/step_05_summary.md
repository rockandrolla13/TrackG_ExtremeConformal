The repository already contains the requested validator in [src/data/validate_data.py](/media/ak/10E1026C4FA6006E/GitRepos/TrackG_ExtremeConformal/src/data/validate_data.py), and it implements the prerequisite gate exactly as specified: if either `data/fx/raw/` or `data/crypto/raw/` has no CSV files, it prints `ERROR: No CSV files found. Downloads must have failed.` and exits non-zero before attempting any data-quality checks. Current verification confirmed that both raw-data directories are still empty, which is consistent with the Step 4 download failure caused by upstream DNS resolution errors.

Execution results for this step are therefore a controlled prerequisite failure rather than a data-validation pass/fail on populated datasets. `python src/data/validate_data.py` exited with code `1` and printed `Overall result: FAIL` after the missing-CSV message, while `python -m pytest tests/test_validate_data.py` passed with `6 passed`, confirming the validator and its prerequisite behavior are covered by tests. No additional data-quality report could be produced because there are still no downloaded CSV inputs to validate.
