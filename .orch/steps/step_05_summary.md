Updated `src/data/validate_data.py` so the prerequisite check now matches the step contract exactly: if either `data/fx/raw/` or `data/crypto/raw/` has no CSV files, it prints `ERROR: No CSV files found. Downloads must have failed.` and exits non-zero before attempting validation. I also extended `tests/test_validate_data.py` with coverage for that prerequisite behavior while keeping the existing checks for date gaps, extreme-event coverage, timezone inference, date bounds, and row-level quality metrics.

Execution results remain blocked by the failed download step. A direct prerequisite check returned `ERROR: No CSV files found. Downloads must have failed.` with exit code `1`, and `python src/data/validate_data.py` produced the same failure followed by `Overall result: FAIL`. Test verification succeeded with `python -m pytest tests/test_validate_data.py` (`6 passed`), while the standalone `pytest` command still points at an environment without `pandas`, so the repository should use `python -m pytest` or align the `pytest` interpreter with the active Python environment.
