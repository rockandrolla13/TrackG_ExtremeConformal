Updated the FX and crypto documentation to reflect the current repository state after the failed download and validation steps. Both README files now record the configured data sources, download procedures, authentication requirements, retry/throttling behavior, expected file names, and the actual raw-file counts observed on 2026-03-12: `0/3` FX files and `0/2` crypto files.

The documentation also summarizes the validation outcome so the failure state is explicit: FX downloads stopped on a DNS resolution error for HistData, crypto downloads never started because the pipeline failed fast, and the validator then failed its prerequisite check because neither raw directory contained any CSV files. The extreme-events CSVs remain in the requested `datetime,event,description` format.
