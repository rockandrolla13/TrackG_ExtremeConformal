I verified the prerequisite counts before documenting and confirmed that the raw-data directories are still empty on 2026-03-12: `data/fx/raw/` contains `0` files instead of the expected `3`, and `data/crypto/raw/` contains `0` files instead of the expected `2`. I updated the README files in `data/fx/` and `data/crypto/` to record the missing CSVs explicitly, preserve the requested date ranges and target filenames, and document the real source implementations from the download scripts: Dukascopy with TrueFX fallback for FX, and Binance Data Vision for crypto.

The documentation now also reflects the actual execution and validation outcomes from the prior steps. The FX download failed immediately because DNS resolution failed for both Dukascopy and TrueFX, the workflow therefore did not attempt the crypto download, and `src/data/validate_data.py` then failed its prerequisite check because neither raw directory contained any CSV files. The extreme-events CSV files remain in place in the requested `datetime,event,description` format.
