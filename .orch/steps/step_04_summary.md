Step 4 executed the download workflow in fail-fast mode after verifying `data/fx/raw` and `data/crypto/raw` existed. The run started with `python src/data/download_fx.py`, but it failed immediately before any file downloads completed. The failure occurred while requesting HistData's token page at `https://www.histdata.com/download-free-forex-data/?/ascii/1-minute-bar-quotes`, where `requests` raised a `ConnectionError` caused by `urllib3.exceptions.NameResolutionError` for host `www.histdata.com`. Because the wrapper used `set -euo pipefail`, execution stopped at that point with exit code `1`, as required by the step instructions.

Post-failure verification showed that both raw-data directories still exist but contain no CSV outputs, so none of the required FX or crypto files were produced and the crypto downloader was not started. Final state after the failed run: `data/fx/raw` contained `0` CSV files totaling `0` bytes, and `data/crypto/raw` contained `0` CSV files totaling `0` bytes. This step should remain marked as failed until network/DNS access to the upstream data sources is available and the download commands can be rerun successfully.
